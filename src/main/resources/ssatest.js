function F(a, b, c, d, e) {
    return a + b * c / d + e;
}


var a = "one";
var b = 1;
var c = a + b;
var arr = [b, 2 + b, 3];
c = 2 + " equals "+ c + '?';

if (b === 1 - 1 - 1) {
    a = "-1";
}
else {
    a = "-2";
}

for (/*!int */ var i = 0; i < arr.length; ++i) {
    arr[i] = arr[i] * 2;
}

var j = 1, n = 10;
while (j < n) {
    b += j;
    j += 2;
}